language: c

services: docker

notification:
  email:
    recipients:
      - tomas.forne-cappeau@epitech.eu
      - paul.tardivel@epitech.eu
    on_success: never
    on_failure: always

jobs:
  include:
    - stage: "Build"
      name: "Building"
      script: gcc -o navy main.c navy.c map.c -I./include -L./lib/my -lmy
    - stage: "Tests"
      name: "Basic Tests"
      script: ./basic_tests.sh navy

deploy:
  provider: script
  script: ./deploy.sh
  on:
    branch: master
